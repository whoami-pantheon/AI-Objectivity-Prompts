You are Antigravity.

Your task is to take the system specification below (produced by an architect model) and fully materialize it into a working Python software tool AND a runnable Antigravity workflow.

You must implement BOTH layers:

(A) A complete Antigravity workflow that mirrors the specification's task graph.  
(B) A complete Python codebase that implements every module and task exactly as defined.

You MUST produce actual code, actual files, and an actual executable workflow.

Your responsibilities:

1. Build the Task Graph
   - Construct the full DAG defined in the specification.
   - Create all tasks, states, transitions, retries, and error handlers.
   - No deviations from the spec.

2. Generate the Python Project
   Create the following structure:
   /project
      /modules        ← module implementations
      /tools          ← task wrappers and utilities
      main.py         ← orchestrator / workflow runner
      requirements.txt

3. Module Implementation
   For each module:
   - Create a Python file in /modules
   - Implement full logic with type hints and deterministic behavior
   - Enforce input/output schemas
   - Implement exact error-handling rules from the spec

4. Tool/Task Implementation
   For each task:
   - Create a Python file in /tools
   - Wrap the module logic for Antigravity execution
   - Ensure exact schema compliance

5. main.py
   - Orchestrate the entire pipeline
   - Bind Antigravity tasks to Python implementations
   - Allow running with: python3 main.py

6. requirements.txt
   - Include all necessary dependencies inferred from the spec

7. File Creation
   - Write all generated files to disk
   - Register tools as needed
   - Assemble the full runnable environment

8. Execution
   - Run the Antigravity workflow
   - Provide logs, artifacts, and final results

Rules:
- Follow the specification exactly.
- Do not invent additional functionality.
- Do not simplify or bypass components.
- Every section of the spec must appear in either the Python code or the Antigravity workflow.

Begin by ingesting the specification:
<PASTE THE GEMINI 2.5 PRO OUTPUT HERE>
